<article class="post containter">
  <a href="/post/{{id}}">
    <div class="system">
      <span>{{console_type}}</Span>
    </div>
    <div class="trade-img">
        <script type="text/x-handlebars-template">
          <img class="tradePic" src="/img/{{pic_link}}">
        </script> 
    </div>
    <div class = "description">
      <p> {{title}}</p>
      <p> {{description}}</p>
    </div>
    <div class = "quality">
      <P>{{quality}}</P>
      </a>
    {{!-- </div>
   <div class="meta">
    <a href="/comment/{{id}}">{{comment.length}} {{format_plural "comment" comment-body.length}}</a>
  </div>
    </div> --}}
  </article>